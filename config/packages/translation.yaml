framework:
    default_locale: '%locale%'
    translator:
        paths:
            - '%kernel.project_dir%/translations'
        fallbacks:
            - '%locale%'
            - 'ru'
            # английский — страховка: ключ без русского перевода покажет английский текст, а не сырой путь
            - 'en'
