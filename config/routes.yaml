app_admin_catalog_item:
    resource: |
        alias: app.catalog_item
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_item
        vars:
            all:
                subheader: app.ui.catalog_item
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_catalog:
    resource: |
        alias: app.catalog
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog
        vars:
            all:
                subheader: app.ui.catalog
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin
    
app_admin_catalog_characteristic:
    resource: |
        alias: app.catalog_characteristic
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_characteristic
        vars:
            all:
                subheader: app.ui.catalog_characteristic
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin


app_admin_catalog_item_characteristic:
    resource: |
        alias: app.catalog_item_characteristic
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_item_characteristic
        vars:
            all:
                subheader: app.ui.catalog_item_characteristic
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_catalog_group:
    resource: |
        alias: app.catalog_group
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_group
        vars:
            all:
                subheader: app.ui.catalog_group
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin
