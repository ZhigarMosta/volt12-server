app_admin_catalog_item:
    resource: |
        alias: app.catalog_item
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_item
        vars:
            all:
                subheader: app.ui.catalog_item
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_catalog:
    resource: |
        alias: app.catalog
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog
        vars:
            all:
                subheader: app.ui.catalog
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin
    
app_admin_catalog_characteristic:
    resource: |
        alias: app.catalog_characteristic
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_characteristic
        vars:
            all:
                subheader: app.ui.catalog_characteristic
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin


app_admin_catalog_item_characteristic:
    resource: |
        alias: app.catalog_item_characteristic
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_item_characteristic
        vars:
            all:
                subheader: app.ui.catalog_item_characteristic
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_catalog_group:
    resource: |
        alias: app.catalog_group
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_group
        vars:
            all:
                subheader: app.ui.catalog_group
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_catalog_item_image:
    resource: |
        alias: app.catalog_item_image
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_catalog_item_image
        vars:
            all:
                subheader: app.ui.catalog_item_image
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_feedback_from_map:
    resource: |
        alias: app.feedback_from_map
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_feedback_from_map
        vars:
            all:
                subheader: app.ui.feedback_from_map
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_service_group:
    resource: |
        alias: app.service_group
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_service_group
        vars:
            all:
                subheader: app.ui.service_group
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_service:
    resource: |
        alias: app.service
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_service
        vars:
            all:
                subheader: app.ui.service
            index:
                icon: 'file image outline'
    type: sylius.resource
    prefix: /admin

app_admin_user:
    resource: |
        alias: app.user
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_user
        path: client_users
        vars:
            all:
                subheader: app.ui.user
            index:
                icon: 'user outline'
    type: sylius.resource
    prefix: /admin

app_admin_cart:
    resource: |
        alias: app.cart
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_cart
        vars:
            all:
                subheader: app.ui.cart
            index:
                icon: 'shopping cart'
    type: sylius.resource
    prefix: /admin

app_admin_compare:
    resource: |
        alias: app.compare
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_compare
        vars:
            all:
                subheader: app.ui.compare
            index:
                icon: 'balance scale'
    type: sylius.resource
    prefix: /admin

app_admin_favorite:
    resource: |
        alias: app.favorite
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show']
        redirect: update
        grid: app_admin_favorite
        vars:
            all:
                subheader: app.ui.favorites
            index:
                icon: 'heart'
    type: sylius.resource
    prefix: /admin

app_admin_user_order:
    resource: |
        alias: app.user_order
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show', 'create']
        redirect: update
        grid: app_admin_user_order
        vars:
            all:
                subheader: app.ui.user_order
            index:
                icon: 'shop'
    type: sylius.resource
    prefix: /admin

app_admin_entity_history:
    resource: |
        alias: app.entity_history
        section: admin
        templates: "@SyliusAdmin\\shared\\crud"
        except: ['show', 'create', 'update']
        redirect: index
        grid: app_admin_entity_history
        vars:
            all:
                subheader: История изменений сущностей
            index:
                icon: 'clock outline'
    type: sylius.resource
    prefix: /admin
