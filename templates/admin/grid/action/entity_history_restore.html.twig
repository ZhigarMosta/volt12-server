<form method="post"
      action="{{ path('app_admin_entity_history_restore', {id: data.id}) }}"
      class="d-inline-flex align-items-stretch m-0">
    <input type="hidden" name="_token" value="{{ csrf_token('restore_' ~ data.id) }}">
    <button type="submit"
            class="btn btn-sm btn-outline-warning"
            onclick="return confirm('Восстановить эту версию?')">
        {{ ux_icon('tabler:arrow-back-up', {class: 'icon'}) }} Восстановить
    </button>
</form>
