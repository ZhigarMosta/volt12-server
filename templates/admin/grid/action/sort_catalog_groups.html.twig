{% set catalog_id = data.id|default(null) %}

{% if catalog_id is not null %}
    {{ sort_modal(
        'name',
        'null',
        false,
        path('admin_crud_sort_catalog_groups'),
        path('admin_crud_all_catalog_groups_by_catalog_id', {'id': catalog_id}),
        'catalog_groups'
    )|raw }}
{% endif %}