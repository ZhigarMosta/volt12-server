{% set order_id = resource.id|default(data.id|default(null)) %}
{% if order_id is not null %}
    <a href="{{ path(options.route_to_item, {'criteria': {'order': order_id}}) }}"
       class="btn">Продукты</a>
{% endif %}
