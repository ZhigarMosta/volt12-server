{% if data.source == constant('App\\Entity\\UserOrder::SOURCE_QUICK') %}
    <span class="badge bg-yellow-lt">{{ data.sourceLabel }}</span>
{% else %}
    <span class="badge bg-secondary-lt">{{ data.sourceLabel }}</span>
{% endif %}
