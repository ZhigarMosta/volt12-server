<div class="container-xl">
    <div class="mb-4">
        <a href="{{ path('app_admin_docs') }}" class="js-docs-banner">
            <div class="js-docs-banner__icon">{{ ux_icon('tabler:book-2') }}</div>
            <div class="js-docs-banner__text">
                <div class="js-docs-banner__title">Документация по админ-панели</div>
                <div class="js-docs-banner__subtitle">
                    Как устроены сущности, CRUD-операции и все нюансы работы с админкой — читайте здесь
                </div>
            </div>
            <div class="js-docs-banner__cta">
                Открыть документацию
            </div>
        </a>
    </div>
</div>

<style>
    .js-docs-banner {
        position: relative;
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 24px 28px;
        border-radius: 16px;
        overflow: hidden;
        text-decoration: none;
        color: #fff;
        background: linear-gradient(
            120deg,
            #1e2433 0%,
            #1e2433 30%,
            rgba(34, 185, 154, 0.8) 48%,
            #adb5bd 62%,
            #1e2433 85%,
            #1e2433 100%
        );
        background-size: 260% 260%;
        animation: js-docs-gradient-move 12s ease infinite;
        box-shadow: 0 8px 24px rgba(30, 36, 51, 0.35);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .js-docs-banner:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 32px rgba(34, 185, 154, 0.3);
        color: #fff;
    }

    .js-docs-banner__icon {
        position: relative;
        z-index: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        width: 56px;
        height: 56px;
        min-width: 56px;
        border-radius: 14px;
        background: rgba(173, 181, 189, 0.18);
        border: 1px solid rgba(34, 185, 154, 0.4);
        backdrop-filter: blur(2px);
        font-size: 28px;
    }

    .js-docs-banner__text {
        position: relative;
        z-index: 1;
        flex: 1;
        min-width: 0;
    }

    .js-docs-banner__title {
        font-size: 20px;
        font-weight: 700;
        line-height: 1.3;
    }

    .js-docs-banner__subtitle {
        margin-top: 2px;
        font-size: 14px;
        opacity: 0.92;
        line-height: 1.4;
    }

    .js-docs-banner__cta {
        position: relative;
        z-index: 1;
        display: flex;
        align-items: center;
        gap: 8px;
        white-space: nowrap;
        font-weight: 600;
        font-size: 14px;
        padding: 10px 18px;
        border-radius: 10px;
        background: rgba(173, 181, 189, 0.2);
        border: 1px solid rgba(34, 185, 154, 0.45);
    }

    @keyframes js-docs-gradient-move {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    @media (max-width: 640px) {
        .js-docs-banner {
            flex-wrap: wrap;
        }
        .js-docs-banner__cta {
            margin-left: 76px;
        }
    }
</style>
